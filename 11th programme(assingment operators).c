/*Assingment Operators*/
/*1. "=" used to store a value in a variable (i.e int a =7 )*/
/*2."+=" used to add any number with previous value (a += 1)= 8*/
/*3."-=" used to substract any number with previous value (a -= 1)= 6**/
/*4. "*="used to multiply any number with previous value (a *= 2)= 14*/
/*5. "/="used to divide any number with previous value (a /= 1)= 7*/


# include<stdio.h>
int main()
{
    int a = 7;
    printf("%d",a);
    a -= 1;
    printf("%d",a);
    return 0;
}
